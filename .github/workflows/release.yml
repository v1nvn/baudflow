name: Release

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [main]

jobs:
  release:
    # Only run when the CI workflow succeeds - never on a failing build
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_PAT }}

      - uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.20"
          otp-version: "29"

      - uses: actions/cache@v6
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-deps-${{ hashFiles('mix.lock') }}
          restore-keys: ${{ runner.os }}-deps-

      - run: mix deps.get

      - name: Configure git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Record version before release
        id: before
        run: MIX_ENV=test mix git_ops.project_info --format github-actions

      - name: Bump version, update changelog, commit, tag
        run: mix git_ops.release --yes --no-major || true
        env:
          MIX_ENV: test

      - name: Record version after release
        id: after
        run: MIX_ENV=test mix git_ops.project_info --format github-actions

      - name: Push version bump and tag
        if: steps.before.outputs.app_version != steps.after.outputs.app_version
        run: git push --follow-tags origin main

      - name: Determine release tag
        if: steps.before.outputs.app_version != steps.after.outputs.app_version
        id: release-tag
        run: echo "tag=$(git describe --tags --abbrev=0)" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        if: steps.before.outputs.app_version != steps.after.outputs.app_version
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.release-tag.outputs.tag }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_PAT }}
